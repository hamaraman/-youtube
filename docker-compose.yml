services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      DB_URL: ${DB_URL:jdbc:mariadb://localhost:3306/mytube}
      DB_USERNAME: root
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      APP_BASE_URL: https://mytube.it.com
      R2_ENDPOINT: https://e1e200052d3d2aa249c0af7b4bdc7d9e.r2.cloudflarestorage.com
      R2_ACCESS_KEY: ${R2_ACCESS_KEY}
      R2_SECRET_KEY: ${R2_SECRET_KEY}
      R2_BUCKET: mytube-videos
      R2_PUBLIC_URL: https://pub-d8c43551d665481cb26bba03837e4eb5.r2.dev
      MAIL_USERNAME: ${MAIL_USERNAME}
      MAIL_PASSWORD: ${MAIL_PASSWORD}
    restart: unless-stopped
